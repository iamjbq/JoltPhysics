#pragma once

namespace JoltPhysics
{
    namespace Editor
    {
        // Registers widget handlers for component UI configurations (e.g. collision layer and group)
        void RegisterPropertyTypes();
        void UnregisterPropertyTypes();
    }
}
